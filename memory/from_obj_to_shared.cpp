#include <iostream>
#include <memory>

using namespace std;

// class A
// {
// public:
//     int i;
//     A(const A&) = delete;
//     A(int _i) : i(_i) {}
// };

int main()
{
    // A a(13);
    
    // shared_ptr<A> _a = make_shared<A>(std::move(a));
    // // shared_ptr<A> _a2 = a;```
    // cout << _a->i;
}